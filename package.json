{
  "name": "yardmanager-phase1",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host=0.0.0.0 --port=3000",
    "build": "tsc -b && vite build",
    "lint": "tsc --noEmit",
    "preview": "vite preview --host=0.0.0.0",
    "seed:masters": "node scripts/seedMasters.mjs",
    "seed:demo": "node scripts/seedMasters.mjs",
    "deploy": "npx firebase-tools deploy --project new-yard-kanri --only hosting,firestore:rules,firestore:indexes",
    "deploy:hosting": "npx firebase-tools deploy --project new-yard-kanri --only hosting"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.4",
    "firebase": "^12.12.1",
    "lucide-react": "^0.546.0",
    "react": "^19.0.1",
    "react-dom": "^19.0.1",
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@types/react": "^19.0.8",
    "@types/react-dom": "^19.0.3",
    "typescript": "~5.8.2"
  }
}
